pipeline {
    agent any

    stages {
        stage('without Docker') {
            steps {
                sh '''
                 echo hello jen                 
                 '''
            }
        }        
    }
}